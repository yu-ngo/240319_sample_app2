<h1>投稿一覧</h1>

<% @lists.each do |list| %>
 
 <p>タイトル</p>
 <!-- <span><%= list.title %></span>を削除して、以下コードを変更 -->
 <span>
   <%= link_to list.title, list_path(list.id) %>
 </span>
 
 <p>画像</p>
 <% if list.image.attached? %>
  <%= image_tag lilst.image, size: "200x️200" %>
 <% else %>
  <%= image_tag 'no_image',size: "200x200" %>
 <% end %>

<% end %>
